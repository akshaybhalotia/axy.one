# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Akshay Bhalotia
email: contact@axy.one
description: >- # this means to ignore newlines until "baseurl:"
  Home page for Akshay Bhalotia — musings, work, and posts.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://axy.one" # base hostname & protocol (used for absolute feed/SEO URLs)
repo_url: "https://github.com/akshaybhalotia/axy.one" # footer "Source" link
author:
  name: Akshay Bhalotia
  # email intentionally omitted: keeps the plain address out of the RSS/Atom feed.
  # The contact page uses the top-level `email` above, obfuscated via obfuscate_email.
twitter_username: akshay_bhalotia
github_username:  akshaybhalotia
linkedin_username: akshaybhalotia
instagram_username: akshay_bhalotia
stackoverflow_username: 3332367/akshay-bhalotia
telegram_username: akshaybhalotia
steam_username: akshaybhalotia
avatar: /assets/img/avatar.png

# Build settings
plugins:
  - jekyll-feed

# Content collections. Posts use the built-in collection; work is custom.
collections:
  work:
    output: true
    permalink: /work/:name

defaults:
  - scope: { path: "", type: work }
    values: { layout: project }
  - scope: { path: "", type: posts }
    values: { layout: post }

# Posts at /posts/<slug> (no date prefix)
permalink: /posts/:title

# RSS: posts feed at /feed.xml, plus a work feed at /work/feed.xml
feed:
  collections:
    work:
      path: /work/feed.xml

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
exclude:
  # build/tooling
  - _tailwind
  - bin
  - script
  - vendor
  - Gemfile
  - Gemfile.lock
  # repo docs (GitHub-facing, not site pages)
  - README.md
  - readme.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - CHANGELOG.md
  - LICENSE
  # restore Jekyll defaults that an explicit `exclude:` would otherwise drop
  - node_modules
  - gemfiles

include:
  - _redirects
